# VEGE PWA Frontend

The main frontend is `index.html`. When hosted on GitHub Pages, it uses `your configured backend URL` as the default backend. When served directly by the Node backend, it uses the same origin.
